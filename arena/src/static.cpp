#include "static.h"

const float AMOUNT_SPRITES = 6.0;
const float ANIMATION_TIME_BASE = 1.0 / 12.0;

const float BASE_SPEED = 500.0;
const float BASE_SCALE = 4.0;

float width = 1280; // GetRenderWidth(); // 640;
float height = 720; // GetRenderHeight(); // 360;
